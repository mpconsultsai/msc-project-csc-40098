# Training / modelling environment (install from repository root)
#
#   python3 -m venv .venv
#   source .venv/bin/activate
#   pip install -r training/requirements.txt
#
# Includes shared project deps (pandas, jupyter, Pillow, etc.) via parent file.

-r ../requirements.txt

# Baselines and metrics
scikit-learn>=1.4.0,<2
scipy>=1.11.0,<2
tqdm>=4.66.0,<5
datasets>=2.16.0,<4

# Deep learning (image baselines, transfer models, fusion)
torch>=2.2.0,<3
torchvision>=0.17.0,<1
transformers>=4.38.0,<5
accelerate>=0.26.0,<2
