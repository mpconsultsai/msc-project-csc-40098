# Pipeline (data-preparation) dependencies — self-contained.
#
# Covers the numbered scripts in pipeline/src/ and the EDA notebook in
# pipeline/notebooks/. Install from the project root:
#
#   python3 -m venv .venv
#   source .venv/bin/activate        # Windows: .venv\Scripts\activate
#   pip install -r pipeline/requirements.txt

# Data handling
pandas>=2.1.0,<3
numpy>=1.26.0,<3

# Image fetch + validation (steps 06, 09)
requests>=2.28.0,<3
Pillow>=10.0.0,<12
ImageHash>=4.3.0,<5

# Fakeddit metadata download (step 02)
gdown>=4.7.0,<6

# FakeNewsNet article crawl (step 01)
newspaper3k>=0.2.8
tqdm>=4.66.0
lxml>=4.9.0
lxml_html_clean>=0.4.0
beautifulsoup4>=4.12.0
twython>=3.9.0

# Exploratory analysis notebook (pipeline/notebooks/)
matplotlib>=3.8.0,<4
seaborn>=0.13.0,<0.14
jupyter>=1.0.0,<2
ipykernel>=6.29.0,<7
