# Gradio PoC — install from project root: .venv/bin/pip install -r ui/requirements.txt
gradio>=4.44.0,<6
torch>=2.0.0
torchvision>=0.15.0
transformers>=4.38.0,<5
# Pin to the Colab training version used to save TF-IDF / late-fusion joblibs (avoids InconsistentVersionWarning).
scikit-learn==1.5.2
joblib>=1.3.0
Pillow>=10.0.0
